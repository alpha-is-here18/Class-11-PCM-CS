<!DOCTYPE html>
<html>
<head>
  <title>Subjects</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      background: #f0f8ff;
      overflow-x: hidden;
    }

    h1, h2 {
      margin-top: 40px;
      color: #333;
    }

    .button-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 20px;
      margin-top: 30px;
    }

    .button-container a, ul li a {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 250px;
      height: 60px;
      font-size: 1.5em;
      padding: 12px 25px;
      background: #4CAF50;
      color: white;
      text-decoration: none;
      border-radius: 8px;
      transition: background 0.3s;
    }

    .button-container a:hover, ul li a:hover {
      background: #388e3c;
    }

    ul {
      list-style: none;
      padding: 0;
    }

    .hidden {
      display: none;
    }

    #contentPanel {
      width: 100vw;
      transform: translateX(100%);
      transition: transform 0.5s ease-in-out;
      position: absolute;
      top: 0;
      left: 0;
      background: #ffffff;
      height: 100vh;
      z-index: 10;
      padding-top: 100px;
    }

    #contentPanel.active {
      transform: translateX(0);
    }

    #closeContent {
      position: absolute;
      top: 20px;
      right: 30px;
      font-size: 1.5em;
      background: #f44336;
      color: white;
      border: none;
      border-radius: 5px;
      padding: 10px 15px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <h1>Choose a Subject</h1>
  <div class="button-container" id="subjectButtons">
    <a href="#" onclick="showChapters('Maths')">📘 Maths</a>
    <a href="#" onclick="showChapters('English')">📗 English</a>
    <a href="#" onclick="showChapters('CS')">💻 Computer Science</a>
    <a href="#" onclick="showChapters('Chemistry')">⚗️ Chemistry</a>
    <a href="#" onclick="showChapters('Physics')">🔬 Physics</a>
  </div>

  <div id="chapters" class="hidden">
    <h2 id="subjectTitle"></h2>
    <div class="button-container" id="chapterList"></div>
    <a href="#" onclick="goHome()">← Back to Subjects</a>
  </div>

  <div id="chapterOptions" class="hidden">
    <h2 id="chapterTitle"></h2>
    <div class="button-container">
      <a href="#" onclick="showContent('notes')">📓 Notes</a>
      <a href="#" onclick="showContent('exercise')">🧠 Exercise</a>
      <a href="#" onclick="showContent('summary')">📝 Summary</a>
    </div>
    <a href="#" onclick="goBackToChapters()">← Back to Chapters</a>
  </div>

  <div id="contentPanel">
    <button id="closeContent" onclick="hideContent()">✖</button>
    <div id="contentDisplay"></div>
  </div>

  <script>
    const data = {
      Maths: ["📘 Algebra", "📐 Geometry", "📊 Trigonometry"],
      English: ["📖 Grammar", "📰 Comprehension", "📚 Literature"],
      CS: ["💡 Programming Basics", "🌐 Web Development", "📦 Data Structures"],
      Chemistry: ["⚛️ Atoms", "🔬 Molecules", "🧪 Reactions"],
      Physics: ["🏃 Motion", "🧲 Forces", "⚡ Electricity"]
    };

    let currentSubject = '';
    let currentChapter = '';

    function showChapters(subject) {
      currentSubject = subject;
      document.getElementById("subjectButtons").classList.add("hidden");
      document.getElementById("chapters").classList.remove("hidden");
      document.getElementById("subjectTitle").innerText = subject + " - Chapters";
      const chapterList = document.getElementById("chapterList");
      chapterList.innerHTML = "";
      data[subject].forEach(chapter => {
        const a = document.createElement("a");
        a.href = "#";
        a.innerText = chapter;
        a.onclick = () => showChapterOptions(subject, chapter);
        chapterList.appendChild(a);
      });
    }

    function showChapterOptions(subject, chapter) {
      currentChapter = chapter;
      document.getElementById("chapters").classList.add("hidden");
      document.getElementById("chapterOptions").classList.remove("hidden");
      document.getElementById("chapterTitle").innerText = subject + " - " + chapter;
    }

    function showContent(type) {
      const contentMap = {
        notes: `<h2>Notes for ${currentChapter}</h2>
                <p>Upload your notes or insert PDF/text content here.</p>`,
        exercise: `<h2>Exercise for ${currentChapter}</h2>
                   <p>Upload questions or practice problems here.</p>`,
        summary: `<h2>Summary of ${currentChapter}</h2>
                  <p>Upload key points and highlights here.</p>`
      };
      document.getElementById("contentDisplay").innerHTML = contentMap[type];
      document.getElementById("contentPanel").classList.add("active");
    }

    function hideContent() {
      document.getElementById("contentPanel").classList.remove("active");
    }

    function goHome() {
      document.getElementById("chapters").classList.add("hidden");
      document.getElementById("subjectButtons").classList.remove("hidden");
    }

    function goBackToChapters() {
      document.getElementById("chapterOptions").classList.add("hidden");
      showChapters(currentSubject);
    }
  </script>
</body>
</html>
